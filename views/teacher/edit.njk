{% extends "layout.njk" %}

{% block content %}
<div class="card">
        <div class="img-url" style="background:url({{teacher.avatar_url}}) no-repeat center center/cover">
        </div>

<section class="put">
    <form  method="POST" action="/teacher?_method=PUT">
        <div class="details">
                <h3>Novo Professor</h3>
                {%include "teacher/fields.njk"%}
                <input type="hidden" name="id"value={{teacher.id}}>

            <div class="btn">
                <button class="salvar" type="submit">Salvar</button>
            </div>
     </form >

    <form   method="POST" action="/teacher?_method=DELETE">
        <div class="btn"></div>
        <button class =delete type="submit">Deletar</button> 
        <input type="hidden" name="id"value={{teacher.id}}>        
    </form>

{% endblock content %}